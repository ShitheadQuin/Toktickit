// Local development only. Every seeded and migrated account starts with this password and
// mustChangePassword = true (specification.md §11, §12). It is not a real credential.
export const LOCAL_DEV_INITIAL_PASSWORD = 'TokTick2026';
