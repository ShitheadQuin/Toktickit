node_modules/
.env
dist/
build/

# BR-24: uploaded attachment files, stored under a server-generated filename - never committed
server/uploads/

# Issue #17: Playwright run output - regenerated on every run, never committed.
# Note artifacts/ is deliberately NOT ignored: labsheet §12 lists
# artifacts/lab-02/screenshots/ as part of the required repository structure, so the
# responsive captures written there are committed (PR #31 review).
e2e/test-results/
e2e/playwright-report/

# Auto-generated by `prisma init` — AI assistant reference docs, not project files
.agents/
.claude/
.windsurf/
skills-lock.json
